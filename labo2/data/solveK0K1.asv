tau = 0.7;
%xsi = -6.303e-3;
K=12;
format long;
syms K1 K0 xsi
xsi = eval(solve(pi*xsi + log(0.02)*sqrt(1-xsi^2),xsi));

[K0,K1] =solve(xsi-(K1*K+1)/(2*sqrt(K0*K*tau)),4/(xsi*sqrt(K*K0/tau))-0.5*tau,...
    -pi*xsi - log(0.02)*sqrt(1-xsi^2), xsi, K0, K1)
